version: "3.8"

services:
  msnet6:
    image: examenbancobasedotnet
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - "5024:5024"


  angular-app:
    image: angularappbancobase:v1 
    build:
      context: ./ClientApp 
      dockerfile: /Dockerfile 
    ports: 
      - 4200:80